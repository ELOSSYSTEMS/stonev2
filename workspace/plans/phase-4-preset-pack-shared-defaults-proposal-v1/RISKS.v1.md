# Phase 4 Preset-Pack Shared-Defaults Proposal Risks v1

1. `Inference`: It would be easy to let homepage starter-structure language slide into implicit collection-template or PDP-template approval if the placeholder rules are not repeated explicitly.
2. `Inference`: It would also be easy to treat the admitted merchandising family as silent approval for blocked `collection behavior` or `product-card behavior` reuse even though `UNIT-137` did not clear those domains.
3. `Inference`: Industry-flavored defaults can drift into fake certainty if they are written as resolved implementation facts rather than shared planning inferences.
4. `Verified`: The mitigation is to keep every preset default homepage-first, keep collection/PDP dependencies explicit and blocked, label preset-specific posture as inference, and route continuation only to another docs-only boundary step.
