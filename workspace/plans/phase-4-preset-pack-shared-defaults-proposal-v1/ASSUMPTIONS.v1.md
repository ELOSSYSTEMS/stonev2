# Phase 4 Preset-Pack Shared-Defaults Proposal Assumptions v1

- `Verified`: `docs/architecture/stonev2-build-roadmap.md` already defines the five-preset lineup and the preset fields that Phase 4 must eventually define.
- `Verified`: `UNIT-137` already concluded that only resolved shared families and shared settings may inform preset planning now, and that collection-dependent and PDP-dependent assumptions must remain blocked or placeholder-bound.
- `Inference`: Because the roadmap already fixes the preset lineup while the shared-family and shared-settings boundary is now explicit, the missing decision is how to express homepage-first defaults honestly rather than whether the preset lineup exists.
