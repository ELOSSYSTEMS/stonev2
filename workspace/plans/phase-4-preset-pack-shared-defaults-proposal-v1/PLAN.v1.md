# Phase 4 Preset-Pack Shared-Defaults Proposal v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to define shared preset defaults for the five roadmap presets only across the preset fields that `UNIT-137` judged ready now or partially ready now, while keeping collection-dependent and PDP-dependent assumptions explicit, blocked, or placeholder-bound.

## Compared Options

### Option A, define homepage-first shared defaults now for all five presets using admitted shared families and admitted shared settings only

- `Inference`: This is the strongest governed move because `UNIT-137` already admitted the shared homepage-first families and shared settings needed for homepage sequence, tone and density posture, homepage-safe merchandising emphasis, homepage-safe trust posture, and homepage starter structure.
- `Inference`: It converts the boundary verdict into explicit preset defaults without pretending that blocked collection or PDP companion surfaces have become resolved.
- `Inference`: It also preserves fail-closed discipline by limiting the output to homepage-safe defaults and explicit placeholders rather than implementation or hidden template assumptions.

### Option B, define full preset defaults including collection-template and PDP-template starter structure now

- `Inference`: This would overreach the evidence because `UNIT-137` explicitly kept collection-template starter structure, PDP-template starter structure, `collection behavior`, and `PDP support behavior` blocked or placeholder-bound.
- `Inference`: It would silently consume deferred `collection companion` and `PDP companion` work through preset wording rather than through a later dedicated decision.

### Option C, pause again instead of defining any preset defaults until deferred companion families are revisited

- `Inference`: This is too strict because `UNIT-137` already concluded that homepage sequence and tone and density defaults are ready now, and that homepage-safe merchandising, trust, support, and starter-content planning is partially ready now.
- `Inference`: It would discard the explicit Phase 4 runway that the sequencing and boundary units just opened.

### Option D, treat shared defaults as enough evidence for preset runtime implementation or implementation-surface approval

- `Inference`: This is explicitly disallowed because the roadmap remains planning-only and this unit is a defaults proposal, not a runtime boundary or implementation package.
- `Inference`: It would also bypass the repo rule that a separate bounded docs-only package must state exact runtime scope before implementation is considered.

## Recommendation

- `Verified`: **Recommend Option A.**
- `Inference`: The next honest Phase 4 move is to define the five preset defaults only where the shared-family and shared-settings evidence is already strong enough, and to leave every collection-dependent or PDP-dependent continuation visibly blocked.

## Exact Shared-Defaults Verdict

- `Verified`: The five roadmap presets can now receive docs-only homepage-first shared defaults across `homepage sequence`, `tone and density defaults`, `homepage-safe merchandising emphasis`, `homepage-safe trust and support assumptions`, and `matching homepage starter content structure`.
- `Verified`: These defaults may use only the admitted shared families from `UNIT-137`: `hero and opening`, `story and editorial`, `merchandising`, `shoppable media`, `trust and support`, `content and blog`, and supporting `utility and layout support`.
- `Verified`: These defaults may use only the admitted shared settings from `UNIT-137`: `brand identity`, `global design system`, `RTL and Hebrew defaults`, and the section-family settings domains `layout and container`, `density and spacing`, `tone and emphasis`, `media behavior`, and `visibility and ordering`.
- `Verified`: These defaults do not authorize `collection companion`, `PDP companion`, `collection behavior`, `PDP support behavior`, `product-card behavior` reuse, runtime schema approval, runtime file scope, or preset rollout.
- `Verified`: Collection-template starter structure and PDP-template starter structure remain blocked or placeholder-bound for all five presets.

## Shared Homepage-Safe Rules For All Five Presets

1. `Verified`: Homepage sequencing must be composed only from admitted homepage-first or homepage-supporting families.
2. `Verified`: Trust and support defaults must stay inside the shared `trust and support` family or supportive `utility and layout support` usage, not inside collection-hosted or PDP-hosted companion wrappers.
3. `Verified`: Merchandising emphasis may use shared family modes such as `category-first`, `promotion-first`, `density-first`, `editorial-commerce`, or `restrained merchandising`, but may not assume collection-toolbar, archive, navigation, or PDP support behavior.
4. `Verified`: Starter content structure in this unit means homepage starter content structure only.
5. `Verified`: Collection-template starter structure and PDP-template starter structure remain explicit placeholders, not hidden follow-ons.
6. `Inference`: Coherence across the pack comes from sequencing and emphasis changes inside one shared vocabulary, not from inventing separate architectures per industry.

## Five-Preset Shared Defaults

| Preset | Homepage sequence using admitted families only | Tone and density defaults using admitted shared settings only | Homepage-safe merchandising emphasis | Homepage-safe trust and support assumptions | Matching homepage starter content structure |
| --- | --- | --- | --- | --- | --- |
| `jewelry` | `hero and opening` -> `story and editorial` -> `merchandising` -> `trust and support` -> `content and blog` | `Inference`: refined tone, relaxed density preset, wider breathing room, elevated surface treatment, restrained CTA emphasis, portrait-to-square media behavior | `Inference`: restrained merchandising with selective `promotion-first` moments, keeping commerce subordinate to aspiration and craft narrative | `Inference`: emphasize reassurance lane, testimonial proof, and help-prompt contact posture on homepage only; do not assume collection support strip or PDP reassurance bands | `Inference`: launch hero, brand or craft story, curated product rail, proof strip or testimonial lane, one editorial teaser strip, with utility spacing only as support |
| `apparel` | `hero and opening` -> `merchandising` -> `shoppable media` -> `story and editorial` -> `trust and support` -> `content and blog` | `Inference`: energetic tone, medium-to-dense rhythm, sharper CTA emphasis, campaign-forward media treatment, faster section cadence | `Inference`: `category-first` merged with `editorial-commerce`, using homepage merchandising to surface outfits, drops, or seasonal groupings without assuming archive tooling | `Inference`: emphasize delivery, sizing-guidance, and service reassurance through shared homepage trust presets only; keep any collection or PDP support logic placeholder-bound | `Inference`: campaign hero, category-entry or curated grid, one shoppable-look story, one editorial or founder/process lane, one reassurance or FAQ/help lane, optional content strip |
| `electronics` | `hero and opening` -> `merchandising` -> `story and editorial` -> `trust and support` -> `content and blog` | `Inference`: precise tone, denser information rhythm, stronger contrast mode, direct CTA emphasis, cleaner container behavior, product-led media ratio | `Inference`: `density-first` with selective `promotion-first` framing, favoring product discovery and clear offer hierarchy on homepage only | `Inference`: emphasize service explanation, FAQ stack, proof cues, and support contact posture on homepage only; do not assume PDP comparison logic or recommendation behavior | `Inference`: product-led hero, featured or comparison-leaning merchandising rail, explainer story lane for setup or ecosystem context, FAQ/help prompt band, one content strip for guides or launches |
| `beauty` | `hero and opening` -> `story and editorial` -> `shoppable media` -> `merchandising` -> `trust and support` -> `content and blog` | `Inference`: soft-but-confident tone, medium density preset, layered editorial spacing, gentle overlay behavior, balanced CTA emphasis, close-up media treatment | `Inference`: `editorial-commerce` with selective `promotion-first` support, keeping regimen or ritual storytelling adjacent to homepage shopping cues | `Inference`: emphasize ingredient or process reassurance, testimonial proof, FAQ/help availability, and gentle service posture on homepage only; keep product-page support assumptions explicit placeholders | `Inference`: campaign or ritual hero, ingredient/process story section, one shoppable routine story, curated merchandising rail, reassurance or testimonial lane, optional journal/content strip |
| `home-lifestyle` | `hero and opening` -> `story and editorial` -> `merchandising` -> `shoppable media` -> `trust and support` -> `content and blog` | `Inference`: warm tone, medium-relaxed density preset, generous section separation, grounded surface treatment, moderate CTA emphasis, room-scale media behavior | `Inference`: `category-first` with restrained editorial layering, allowing room scenes or styling stories without converting homepage into a collection wrapper | `Inference`: emphasize service clarity, delivery/help reassurance, testimonial or logo proof, and support contact posture on homepage only; keep collection/PDP helper surfaces placeholder-bound | `Inference`: lifestyle hero, room or routine story lane, category-entry merchandising, one shoppable inspiration scene, reassurance/help lane, one editorial teaser strip |

## Shared Placeholder Rules That Still Apply To Every Preset

1. `Verified`: `Collection behavior` remains blocked for every preset.
2. `Verified`: `PDP support behavior` remains blocked for every preset.
3. `Verified`: `Collection-template starter structure` remains an explicit placeholder for every preset.
4. `Verified`: `PDP-template starter structure` remains an explicit placeholder for every preset.
5. `Verified`: `Collection companion` and `PDP companion` wrappers remain blocked and must not be described as if Phase 4 planning resolved them.
6. `Verified`: `Product-card behavior` reuse remains unapproved and must not be treated as cleared because the merchandising family is admitted at the planning level.

## Why The Defaults Stay Coherent Without Reopening Companion Work

1. `Verified`: The roadmap already fixes the preset fields to homepage sequence, tone and density defaults, relevant merchandising emphasis, trust and support assumptions, and matching starter content structure.
2. `Verified`: `UNIT-137` already concluded that those fields are fully or partially ready now only in shared homepage-safe form.
3. `Verified`: The section-family ledger already says breadth should come from normalized families plus presets rather than from separate per-industry architectures.
4. `Inference`: Because each preset in this package varies only sequencing, tone, density posture, and homepage emphasis inside admitted families, the lineup stays coherent without hidden collection or PDP dependence.
5. `Inference`: The explicit placeholders preserve honesty about what is still unresolved rather than flattening those gaps into fake completion.

## Why Runtime Authorization Still Fails Closed

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` remains planning-only.
2. `Verified`: This proposal defines defaults and placeholders only; it does not define runtime file scope, schema field names, or implementation order.
3. `Inference`: Even with coherent shared defaults, a later unit would still need to prove that any runtime-facing preset slice can be bounded without consuming blocked collection or PDP domains.
4. `Verified`: Preset defaults in this package are not implementation approval.

## What This Proposal Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No edits under `project/`.
- `Verified`: No broader `collection companion` review.
- `Verified`: No broader companion-family review.
- `Verified`: No support-strip implementation admission.
- `Verified`: No collection-derived strip content.
- `Verified`: No collection-template rewrites.
- `Verified`: No archive controls.
- `Verified`: No navigation-system behavior.
- `Verified`: No collection-banner authority.
- `Verified`: No curated-explainer or editorial drift.
- `Verified`: No `PDP companion` reopening.
- `Verified`: No broader motion/media scope.
- `Verified`: No preset-pack implementation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce continuation.
- `Verified`: No `product-card behavior` reuse as an implied next step.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Require the next step to stay homepage-first and fail closed on collection-template and PDP-template assumptions unless fresh evidence exists.
- `Verified`: Require the next step to decide whether any exact preset-pack runtime-facing boundary can be stated without consuming blocked `collection behavior`, `PDP support behavior`, `collection companion`, or `PDP companion` domains.
- `Verified`: Require the next step to keep preset defaults distinct from implementation approval.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next step authorizes runtime edits directly.
- `Verified`: Stop if it treats deferred `collection companion` or `PDP companion` work as implicitly resolved, available, or consumed.
- `Verified`: Stop if it fills `collection behavior`, `PDP support behavior`, `collection-template starter structure`, or `PDP-template starter structure` as if companion-family evidence already exists.
- `Verified`: Stop if it reopens broader `collection companion`, broader companion-family review, support-strip implementation admission, collection-derived strip content, collection-template rewrites, archive controls, navigation-system behavior, collection-banner authority, curated-explainer/editorial drift, broader motion/media scope, preset-pack implementation, wider JS, heavier commerce, or `product-card behavior` reuse as approved follow-ons.

## Next Approved Planning Step

- `Verified`: The next governed unit after this proposal should remain docs-only and author one **Phase 4 homepage-first preset-pack implementation-boundary proposal**.
- `Inference`: That proposal is justified because this package demonstrates that the five presets can now be expressed coherently on homepage-first surfaces through admitted shared families and settings alone, while the unresolved continuation is whether an exact homepage-only runtime-facing preset scope can be described without consuming blocked collection or PDP domains.
- `Inference`: That proposal should determine:
  - whether one exact homepage-only preset-pack runtime-facing boundary can be stated without using blocked companion families or blocked commerce behavior domains
  - which shared homepage surfaces, if any, are specific enough for a later bounded implementation package
  - which preset assumptions must remain documentation-only until deferred companion-family evidence changes

## Readiness Verdict

- `Verified`: READY as a docs-only Phase 4 preset-pack shared-defaults proposal.
- `Inference`: The next governed unit after this proposal should remain docs-only and author a Phase 4 homepage-first preset-pack implementation-boundary proposal.
