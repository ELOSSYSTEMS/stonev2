# Phase 4 Homepage Runtime Admission Boundary Handoff v1

- `Verified`: This pre-authored package is conditional on `UNIT-142` passing.
- `Verified`: The package must decide whether one exact homepage runtime proof slice can now be admitted.
- `Verified`: The expected admitted boundary, if justified, is `stone-base.css`, `stone-hero-opening.liquid`, `index.json`, and the two locale files only.
- `Verified`: The package must keep shell/navigation, collection, PDP, editorial/search, settings-surface changes, and preset rollout out of scope.
- `Verified`: If the package passes, the only intended next unit is `UNIT-144`.
