# Phase 4 Homepage Runtime Admission Boundary Assumptions v1

- `Verified`: `UNIT-139` already stated one homepage-only preset composition/defaults contract.
- `Inference`: The smallest honest runtime proof is a neutral homepage host slice rather than a full preset rollout.
- `Inference`: Hero/opening is the most obvious missing homepage-first family in `project/`.
- `Verified`: Shell/navigation, collection, PDP, editorial/search, and preset realization still need separate reopen decisions.
