# Phase 4 PDP Host And Behavior Reopen Evidence Assumptions v1

- `Verified`: Prior state explicitly deferred `PDP companion` behind later planning rather than resolving it.
- `Inference`: A later PDP reopen should start from product host and behavior rather than broad support-family breadth.
- `Inference`: Homepage, shell, and likely collection lanes should resolve first so PDP decisions are not made without surrounding structure.
