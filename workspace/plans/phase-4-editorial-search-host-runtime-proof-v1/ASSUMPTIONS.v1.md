# Phase 4 Editorial Search Host Runtime Proof Assumptions v1

- `Verified`: `UNIT-165` is allowed only if `UNIT-164` passes.
- `Verified`: The proof must stay inside the exact nine runtime files.
- `Inference`: Native Shopify blog, article, and search objects are enough to prove host completion before richer editorial/search behavior.
