# Phase 3 Companion Systems Controlled Expansion Proposal Assumptions v1

- `Verified`: `UNIT-126` already established that the next honest higher-boundary planning problem is a companion-systems question rather than another shoppable-media or motion/media continuation.
- `Verified`: `docs/architecture/stonev2-build-roadmap.md` still places `collection and PDP companion systems` in Phase 3 ahead of Phase 4 preset-pack work.
- `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` keeps `collection companion` and `PDP companion` as distinct families with different risk profiles.
- `Verified`: `UNIT-071` through `UNIT-078` proved bounded collection- and PDP-aware slices, but those proofs did not authorize a broader Phase 3 reopening by themselves.
- `Inference`: `Collection companion` is the safer first reopened planning family because it is less commerce-adjacent than `PDP companion`.
- `Verified`: Broader motion/media scope, collection-template runtime work, PDP-template runtime work, preset-pack implementation, wider JS, and heavier commerce remain disallowed unless a later docs-only boundary says otherwise explicitly.
