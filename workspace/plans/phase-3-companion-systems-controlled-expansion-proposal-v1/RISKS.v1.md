# Phase 3 Companion Systems Controlled Expansion Proposal Risks v1

1. `Inference`: The proposal could accidentally authorize template-coupled runtime work instead of keeping the decision docs-only.
2. `Inference`: The proposal could reopen `collection companion` and `PDP companion` together and hide their different risk profiles.
3. `Inference`: The proposal could jump too quickly to `PDP companion`, which would increase buy-box, recommendation, gallery, and heavier commerce pressure.
4. `Inference`: The proposal could skip unresolved Phase 3 companion-family decisions and smuggle preset-pack sequencing in as an escape hatch.
5. `Verified`: The mitigation is to keep this unit docs-only, reopen planning only through a collection-first docs-only boundary, and preserve explicit stop conditions against template rewrites, broader JS, motion/media drift, and heavier commerce.
