# Phase 3 Collection Support Strip Runtime Boundary v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to determine whether one exact runtime-facing `collection support strip` boundary can now be stated for an editor-authored collection-page-hosted support strip without authorizing runtime implementation.

## Compared Options

### Option A, state one exact runtime-facing support-strip boundary as a future contract only and continue only to a docs-only frontier review

- `Inference`: This is the narrowest evidence-backed move that preserves the `UNIT-131` readiness result without converting readiness into implementation approval.
- `Inference`: It lets the repo name the exact future contract that any later implementation-admission discussion would have to preserve while still failing closed on runtime files, schema keys, host surfaces, and collection-derived behavior.

### Option B, convert the runtime-facing boundary into direct runtime authorization

- `Inference`: This fails the unit directly.
- `Inference`: It would collapse a contract-definition step into implementation approval and would implicitly authorize runtime work that this unit was explicitly told to keep fail-closed.

### Option C, state the boundary loosely enough to permit collection-derived or hybrid collection-system behavior

- `Inference`: This would overreach beyond the evidence.
- `Inference`: It would blur the strip into collection-template authority, archive controls, navigation-system behavior, banner framing, curated-explainer/editorial drift, or merchandising logic, all of which remain explicitly blocked.

### Option D, refuse to state any runtime-facing boundary at all

- `Inference`: This is the safest fallback.
- `Inference`: It is more conservative than the evidence requires because `UNIT-130` and `UNIT-131` already narrowed the strip to one editor-authored collection-page-hosted support case strongly enough to state an exact future contract at the docs-only level.

## Recommendation

- `Verified`: **Recommend Option A.**
- `Inference`: One exact runtime-facing boundary can now be stated, but only as a future contract definition that still leaves runtime implementation unapproved.

## Runtime-Facing Boundary Verdict

- `Verified`: Stone V2 now has enough evidence to state one exact runtime-facing `collection support strip` boundary for an editor-authored collection-page-hosted support strip.
- `Verified`: The exact runtime-facing boundary is justified only at the docs-only contract-definition level.
- `Verified`: This package does **not** authorize runtime implementation, runtime files, or schema work.
- `Inference`: The honest output of this unit is a precise future contract and a fail-closed handoff, not a build slice.

## Why This Exact Runtime-Facing Boundary Can Be Stated

1. `Verified`: `UNIT-130` already defined one exact support-strip distinctness case as a compact collection-page-first support band and kept banner, explainer, navigation, and merchandising drift blocked.
2. `Verified`: `UNIT-131` already concluded that the only future contract ready enough to evaluate is an **editor-authored collection-page-hosted** support strip with `support-strip visibility` treated narrowly.
3. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` keeps `collection support strip` inside `collection companion`, not inside merchandising or `PDP companion`, and constrains the family to wrapper-like support blocks rather than product-driven contracts.
4. `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` gives the lane one vocabulary clue through `support-strip visibility` while still withholding approval for broader `collection behavior`.
5. `Verified`: `docs/architecture/stonev2-section-block-platform.md` favors normalized family coverage and new runtime files only when behavior is materially different, which supports naming a narrow future contract without widening the family.
6. `Verified`: `docs/architecture/stonev2-build-roadmap.md` still places unresolved collection companion work in Phase 3 ahead of preset-pack implementation, so one more bounded contract-definition step remains phase-consistent.
7. `Inference`: Together, these sources now justify one exact future runtime contract, but not implementation authorization.

## Exact Future Runtime-Facing Boundary

- `Verified`: The only exact future boundary that may be carried forward is one **editor-authored support strip hosted on collection pages**.
- `Verified`: The strip must remain a compact additive support band rather than a collection-surface-defining system.
- `Verified`: The strip may contain only:
  1. one short support-oriented `heading`
  2. one short support-oriented `richtext` message
  3. up to three compact `trust cue` or `feature-detail` items
  4. one optional neutral support CTA inside a `button group`
  5. optional `divider` or `spacer` framing only
- `Verified`: `support-strip visibility` may be referenced only as a narrow presence/exposure concept for whether the strip appears.
- `Verified`: The strip contract remains editor-authored rather than collection-derived.
- `Verified`: The strip contract remains collection-page-hosted rather than archive-system-derived, banner-led, navigation-led, or product-card-led.

## Exact Runtime-Facing Assumptions Any Later Implementation Admission Must Preserve

1. `Verified`: Content stays editor-authored instead of being synthesized from collection metadata, tags, rules, or product sets.
2. `Verified`: Hosting stays collection-page-first without turning the strip into collection-template authority.
3. `Verified`: `support-strip visibility` stays reference-only and presence-oriented rather than becoming a gateway to broader `collection behavior`.
4. `Verified`: The strip stays compact, support-oriented, and additive instead of becoming hero, campaign, or editorial-opening content.
5. `Verified`: Trust cues, feature details, and one neutral support CTA remain enough to express the strip without depending on merchandising or product-card contracts.

## Exact Runtime-Facing Surfaces That Must Remain Fail-Closed

- `Verified`: No collection-derived strip content.
- `Verified`: No collection-template rewrites or template-opening authority.
- `Verified`: No collection archive controls, including filtering, sorting, search, pagination, toolbar behavior, or archive-state logic.
- `Verified`: No navigation-system behavior.
- `Verified`: No `collection banner` authority.
- `Verified`: No curated-explainer or broader editorial/story authority.
- `Verified`: No `PDP companion` reopening.
- `Verified`: No broader motion/media continuation.
- `Verified`: No preset-pack implementation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce continuation.
- `Verified`: No `product-card behavior` reuse, product loops, recommendation logic, or other merchandising dependencies.

## What This Runtime-Facing Boundary Clarifies

- `Verified`: A later support-strip implementation discussion, if it ever occurs, must be judged against an authored support contract rather than against collection-derived behavior.
- `Verified`: Collection-page hosting does not by itself grant collection-template authority.
- `Verified`: `support-strip visibility` does not by itself grant toolbar semantics, density presets, merchandising emphasis, or navigation behavior.
- `Inference`: The strip remains coherent only while it stays smaller than banner, explainer, navigation, and merchandising systems.

## Why Runtime Implementation Still Fails Closed

- `Verified`: This boundary defines a future contract, not an implementation slice.
- `Inference`: The current canon still does not justify exact runtime file scope, exact schema key scope, exact host surface selection, or exact Liquid/CSS edit authority for this strip.
- `Inference`: Approving implementation here would skip the decision point that asks whether support-strip planning is already locally exhausted at the contract-definition edge or whether any later implementation-admission step is honest at all.

## Next Approved Planning Step

- `Verified`: The next governed unit after this boundary should remain docs-only and author one **Phase 3 post-collection-support-strip runtime-boundary frontier review**.
- `Inference`: That frontier review should determine:
  - whether support-strip planning is now locally exhausted at the docs-only runtime-boundary edge
  - whether any later support-strip implementation-admission proposal is justified at all
  - whether the repo should remain paused rather than infer a runtime slice from this boundary
  - whether broader collection-companion continuation should stay blocked unless fresh evidence appears

## Why The Other Options Are Rejected

- `Verified`: Reject Option B because this unit must fail closed on runtime authorization and cannot turn contract definition into build approval.
- `Verified`: Reject Option C because the canon does not justify collection-derived, archive-derived, banner-derived, editorial-derived, or merchandising-derived strip behavior.
- `Verified`: Reject Option D because the `UNIT-130` plus `UNIT-131` chain now provides enough evidence to state one exact future contract even though implementation remains blocked.

## What This Boundary Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No exact runtime file list.
- `Verified`: No exact schema contract.
- `Verified`: No exact host template or section selection.
- `Verified`: No banner, curated-explainer, navigation, or `PDP companion` continuation.
- `Verified`: No broader collection-system reopening.
- `Verified`: No preset-pack, broader JS, motion/media, heavier commerce, or `product-card behavior` continuation.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Keep the next step support-strip-specific and fail-closed on runtime implementation.
- `Verified`: Require the next step to decide whether support-strip planning is exhausted or whether one later implementation-admission proposal is honestly justified.
- `Verified`: Require the next step to keep the strip contract editor-authored and collection-page-hosted.
- `Verified`: Require the next step to keep `support-strip visibility` narrow and reference-only.
- `Verified`: Require the next step to restate blocked surfaces clearly: collection-derived content, collection-template rewrites, archive controls, navigation-system behavior, collection-banner authority, curated-explainer/editorial drift, `PDP companion`, broader motion/media, preset-pack implementation, wider JS, heavier commerce, and `product-card behavior` reuse.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next step authorizes runtime edits directly.
- `Verified`: Stop if it treats collection-derived strip content, template authority, archive-state behavior, or navigation-system behavior as part of the strip contract.
- `Verified`: Stop if it normalizes collection-banner authority, curated-explainer/editorial drift, `PDP companion`, broader motion/media, preset-pack implementation, wider JS, heavier commerce, or `product-card behavior` reuse as implicit next steps.
- `Verified`: Stop if it infers an implementation slice from this boundary without fresh evidence.

## Readiness Verdict

- `Verified`: READY as a docs-only exact runtime-facing boundary package.
- `Verified`: NOT READY for runtime implementation.
- `Inference`: The intended next execution target is `UNIT-133`, a docs-only post-collection-support-strip runtime-boundary frontier review.
