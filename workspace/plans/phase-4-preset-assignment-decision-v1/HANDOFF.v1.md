# Phase 4 Preset Assignment Decision Handoff v1

- `Verified`: Apparel is assigned to the shared PDP size-chart capability at the decision level.
- `Verified`: Electronics is assigned to the shared PDP spec-sheet capability at the decision level.
- `Verified`: Jewelry, beauty, and home-lifestyle omit size chart and spec sheet by default at the decision level.
- `Verified`: Collection filters remain shared and native for all presets.
- `Verified`: Runtime preset composition remains blocked.
- `Verified`: The missing runtime inputs are exact template mutation policy, exact section order/settings, starter content defaults, and global preset selector behavior.
- `Verified`: No runtime edits are approved after this package.
- `Inference`: Resume only after the owner approves the missing inputs or explicitly overrides the hard block.
