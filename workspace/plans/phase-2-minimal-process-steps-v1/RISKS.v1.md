# Phase 2 Minimal Process Steps Risks v1

## Primary Risks

1. `Inference`: Semantic drift could turn a restrained process sequence into reassurance messaging, service guarantees, or campaign process framing.
2. `Inference`: Interaction drift could push the slice toward accordions, tabs, sliders, or JS-driven step behavior.
3. `Inference`: Presentation drift could push the section toward icons, illustrations, or decorative systems that are not required for the proof.
4. `Inference`: Protected-surface drift could reopen the shared section shell or other protected runtime surfaces.
5. `Inference`: Locale drift could create schema mismatches between Hebrew and English.

## Mitigations

- `Verified`: Keep the planned runtime surface restricted to the same five-file pattern used by the recent slices.
- `Verified`: Reuse the shared section shell unchanged.
- `Verified`: Keep the future template neutral and isolated.
- `Verified`: Fail closed if implementation needs accordion behavior, tabs, icons, illustrations, campaign semantics, or JS.
- `Verified`: Update locale files together and verify structural parity before accepting the slice.

## Replanning Threshold

- `Verified`: Replan immediately if the process-steps section cannot remain a static, plain, editorial sequence inside the approved five runtime files.
