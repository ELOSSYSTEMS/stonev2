# Phase 4 Homepage Runtime Proof Slice Handoff v1

- `Verified`: This pre-authored runtime slice is conditional on `UNIT-143` passing.
- `Verified`: The slice must stay inside the exact five-file homepage proof boundary.
- `Verified`: The slice must add one reusable hero/opening section and one first `index.json` host only.
- `Verified`: The slice must not widen into shell/navigation, collection, PDP, editorial/search, or preset rollout work.
- `Verified`: If the slice passes, the only intended next unit is `UNIT-145`.
