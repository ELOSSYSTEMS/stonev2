# Phase 4 Global Shell And Navigation Reopen Boundary Assumptions v1

- `Verified`: `UNIT-145` passed and verified the exact five-file homepage runtime proof.
- `Verified`: Shell and navigation remain distinct from collection/archive behavior and PDP behavior.
- `Verified`: Current runtime evidence shows a base shell in `theme.liquid`, but no header, footer, primary navigation, utility navigation, search entry, cart entry, or locale-switch shell.
- `Inference`: A verified homepage proof makes global shell authority the next structural dependency.
- `Inference`: Search entry can be considered a shell concern only if search result behavior and predictive search remain blocked.
- `Inference`: The next boundary should be docs-only because `UNIT-146` does not itself authorize runtime implementation.
