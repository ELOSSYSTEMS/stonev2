# Phase 4 Preset-Pack Boundary Proposal Assumptions v1

- `Verified`: `docs/architecture/stonev2-build-roadmap.md` already defines the initial five-preset lineup and the roadmap fields each preset should eventually define.
- `Verified`: `UNIT-135` and `UNIT-136` already concluded that unresolved `collection companion` and `PDP companion` work stay explicitly deferred while Phase 4 planning begins through a docs-only boundary-first pass.
- `Inference`: Because the lineup and shared vocabularies already exist, the missing decision is not whether preset planning may start, but which shared inputs are admissible now and which preset-field dependencies must remain blocked or placeholder-bound.
