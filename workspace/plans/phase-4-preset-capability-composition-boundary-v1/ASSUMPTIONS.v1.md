# Phase 4 Preset Capability Composition Boundary Assumptions v1

- `Verified`: The user wants blocks, sections, PDP capabilities, and collection/filter capabilities stabilized before final per-preset composition.
- `Verified`: Apparel should be able to use size-chart capability and electronics should be able to use spec-sheet capability later.
- `Verified`: Shared collection filters now exist as a native no-JS capability.
- `Inference`: Preset differences should be assigned by selecting shared sections and settings, not by creating per-preset architectures.
- `Inference`: Exact starter content remains necessary before runtime template JSON mutation is safe.
