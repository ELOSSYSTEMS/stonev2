# Phase 3 Collection Support Strip Boundary Risks v1

1. `Inference`: The exact support-strip case could still drift into banner authority if it starts depending on images, hero-scale framing, or opening treatment.
2. `Inference`: The strip could still drift into curated-explainer/editorial authority if the copy contract becomes longform, story-led, or composition-heavy.
3. `Inference`: The shared `collection behavior` vocabulary could be misread as approval for collection toolbar behavior, density presets, or merchandising emphasis instead of only `support-strip visibility`.
4. `Inference`: The strip could still collapse into archive or navigation behavior if CTA language starts acting like collection controls rather than support actions.
5. `Inference`: The lane could also drift into merchandising if it starts depending on `product-card behavior`, product loops, recommendation logic, or heavier commerce framing.
6. `Verified`: The mitigation is to keep this unit docs-only, define the exact strip case as a compact support band, exclude banner/editorial/navigation/product-card authority, and hand off only to a docs-only runtime-readiness proposal.
