# Phase 2 Minimal Story Split Assumptions v1

## Confirmed Assumptions

- `Verified`: `UNIT-050` completed and advanced the repo back to a documentation-only planning boundary.
- `Verified`: The next approved planning surface remains limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: The trust-family micro-slice chain already covers FAQ, reassurance, help prompt, logo proof, testimonial proof, and stat proof.
- `Verified`: The section-family ledger explicitly supports story-and-editorial family work and normalizes image-plus-copy layouts as a separate family from trust/support.

## Working Assumptions

- `Inference`: A restrained story split is the next smallest cross-family proof that stays materially distinct from the verified utility and trust slices.
- `Inference`: The story split can stay static and non-JS while still proving a reusable media-plus-copy pattern.
- `Inference`: The shared section shell remains sufficient for this proof and should not require any snippet changes.
- `Inference`: Bilingual locale additions for a story split can remain structurally aligned without expanding the editor vocabulary beyond existing normalized patterns.

## Assumptions To Re-check During Implementation

- `Inference`: Split-layout styling can remain bounded to `project/assets/stone-base.css` without reopening global token work.
- `Inference`: The section can remain editorial and neutral without drifting into hero, campaign, or merchandising semantics.
- `Inference`: The future implementation can use one neutral host template only.

## Failure Triggers

- `Verified`: If the future slice requires video, gallery logic, hotspot behavior, sliders, overlays, or any JS-driven media treatment, this plan should be treated as invalid.
- `Verified`: If the future slice requires protected-surface edits, `project/blocks/`, a new snippet, a second section, or a second template, this plan should be treated as invalid.
- `Verified`: If the section starts behaving like a hero, blog feed, merchandising surface, or shoppable-media layout, this plan should be treated as invalid.
