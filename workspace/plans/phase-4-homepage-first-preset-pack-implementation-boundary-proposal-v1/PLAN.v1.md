# Phase 4 Homepage-First Preset-Pack Implementation-Boundary Proposal v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to determine whether one exact homepage-only preset-pack runtime-facing boundary can now be stated from the shared-defaults evidence without consuming blocked `collection companion`, `PDP companion`, `collection behavior`, `PDP support behavior`, `collection-template starter structure`, or `PDP-template starter structure`.

## Compared Options

### Option A, state one exact homepage-only preset-pack runtime-facing boundary as a future contract only and continue only to a docs-only frontier review

- `Inference`: This is the strongest governed move because `UNIT-138` already proved that the five roadmap presets can stay coherent through homepage-first sequence, tone and density posture, homepage-safe merchandising emphasis, homepage-safe trust posture, and homepage starter structure alone.
- `Inference`: It preserves the difference between a preset composition contract and runtime implementation approval.
- `Inference`: It also keeps collection and PDP domains visibly blocked instead of letting them leak back in through starter-structure or behavior wording.

### Option B, convert the homepage-first shared-defaults result into direct preset-pack runtime authorization

- `Inference`: This fails the unit directly.
- `Inference`: It would collapse a docs-only contract-definition step into implementation approval and would bypass the explicit requirement to fail closed on runtime authorization.

### Option C, widen the boundary until it includes collection-template defaults, PDP-template defaults, or broader commerce behavior

- `Inference`: This would overread the evidence.
- `Inference`: `UNIT-137` and `UNIT-138` explicitly kept `collection behavior`, `PDP support behavior`, `collection-template starter structure`, and `PDP-template starter structure` blocked or placeholder-bound, so widening the boundary would silently consume deferred companion-family work.

### Option D, refuse to state any homepage-only runtime-facing boundary at all

- `Inference`: This is the safest fallback.
- `Inference`: It is stricter than the evidence requires because the roadmap, ledger, taxonomy, and `UNIT-138` together now support one exact homepage-only preset contract even though they still do not support implementation admission.

## Recommendation

- `Verified`: **Recommend Option A.**
- `Inference`: One exact homepage-only preset-pack runtime-facing boundary can now be stated, but only as a future contract-definition package that keeps runtime implementation, file scope, schema scope, and blocked companion-family domains fail-closed.

## Runtime-Facing Boundary Verdict

- `Verified`: Stone V2 now has enough evidence to state one exact homepage-only preset-pack runtime-facing boundary.
- `Verified`: The exact runtime-facing boundary is justified only at the docs-only contract-definition level.
- `Verified`: This package does **not** authorize runtime implementation, runtime files, runtime schema, or preset rollout.
- `Inference`: The honest output of this unit is one precise homepage preset contract and a fail-closed handoff, not a build slice.

## Why This Exact Homepage-Only Boundary Can Be Stated

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` already fixes the five-preset lineup and says each preset should define `homepage sequence`, `tone and density defaults`, `relevant merchandising emphasis`, `trust and support assumptions`, and `matching starter content structure`.
2. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` says Stone V2 breadth should come from normalized families plus presets rather than separate architectures or uncontrolled file growth.
3. `Verified`: The ledger also says to use a preset when the underlying structure is the same and only the default content pattern, tone, density, or merchandising emphasis changes.
4. `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` says shared vocabularies and preset selectors should drive repeated choices such as density, spacing, tone, card treatment, and visibility behavior.
5. `Verified`: `UNIT-137` already admitted only resolved shared families and shared settings as valid preset-pack inputs now, while keeping collection-dependent and PDP-dependent assumptions blocked or placeholder-bound.
6. `Verified`: `UNIT-138` already proved the five roadmap presets can be expressed coherently through homepage-first shared defaults only, while keeping `collection behavior`, `PDP support behavior`, `collection-template starter structure`, and `PDP-template starter structure` explicit placeholders.
7. `Inference`: Together, these sources now justify one exact homepage-only preset composition/defaults contract, but not any implementation approval beyond that.

## Exact Future Homepage-Only Runtime-Facing Boundary

- `Verified`: The only exact future boundary that may be carried forward is one **homepage-only preset-pack composition and defaults layer** for the five roadmap presets.
- `Verified`: The boundary may bind each preset only to:
  1. one homepage sequence composed only from admitted homepage-first or homepage-supporting families
  2. one shared-selector default posture for tone, density, layout emphasis, media treatment, and CTA emphasis using admitted shared settings only
  3. one homepage-safe merchandising emphasis mode that stays inside shared merchandising vocabulary only
  4. one homepage-safe trust and support posture that stays inside shared homepage trust and support vocabulary only
  5. one homepage starter content skeleton composed only from admitted homepage-first or homepage-supporting families
- `Verified`: The boundary remains preset-driven and homepage-only rather than collection-template-driven, PDP-template-driven, or separate-architecture-driven.
- `Verified`: The boundary remains a shared-family composition/defaults contract rather than a new section-family, new commerce behavior family, or new companion-family wrapper.

## Exact Runtime-Facing Assumptions Any Later Implementation Admission Must Preserve

1. `Verified`: Homepage composition must use only admitted shared families: `hero and opening`, `story and editorial`, `merchandising`, `shoppable media`, `trust and support`, `content and blog`, and supporting `utility and layout support`.
2. `Verified`: Default posture must stay inside admitted shared settings vocabularies: `brand identity`, `global design system`, `RTL and Hebrew defaults`, and the section-family settings domains `layout and container`, `density and spacing`, `tone and emphasis`, `media behavior`, and `visibility and ordering`.
3. `Verified`: Merchandising emphasis must stay homepage-safe and selector-based rather than relying on `collection behavior`, archive controls, toolbars, navigation behavior, or `product-card behavior` reuse approval.
4. `Verified`: Trust and support posture must stay homepage-safe and family-level rather than being expressed through `collection companion`, `PDP companion`, collection-hosted support behavior, or PDP-hosted support behavior.
5. `Verified`: Starter content structure in this boundary means homepage starter structure only; `collection-template starter structure` and `PDP-template starter structure` remain explicit placeholders for every preset.
6. `Verified`: Preset breadth must come from sequence, selector defaults, emphasis, and homepage starter structure changes rather than new per-preset architectures or silent family expansion.

## Exact Runtime-Facing Surfaces That Must Remain Fail-Closed

- `Verified`: No `collection companion` reopening.
- `Verified`: No `PDP companion` reopening.
- `Verified`: No `collection behavior` domain use.
- `Verified`: No `PDP support behavior` domain use.
- `Verified`: No `collection-template starter structure` fill-in.
- `Verified`: No `PDP-template starter structure` fill-in.
- `Verified`: No collection-template rewrites or archive controls.
- `Verified`: No navigation-system behavior.
- `Verified`: No collection-banner authority.
- `Verified`: No support-strip implementation admission.
- `Verified`: No collection-derived strip content.
- `Verified`: No curated-explainer or broader editorial drift.
- `Verified`: No broader motion/media continuation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce continuation.
- `Verified`: No `product-card behavior` reuse approval or broader merchandising-behavior reuse program.

## What This Runtime-Facing Boundary Clarifies

- `Verified`: A later preset-pack implementation discussion, if it ever occurs, must be judged against one homepage-only preset composition/defaults contract rather than against implied collection or PDP behavior.
- `Verified`: Homepage starter structure does not by itself grant collection-template or PDP-template authority.
- `Verified`: Homepage-safe merchandising emphasis does not by itself grant `collection behavior` or `product-card behavior` reuse approval.
- `Verified`: Homepage-safe trust and support posture does not by itself grant `collection companion`, `PDP companion`, or support-strip implementation admission.
- `Inference`: Preset-pack planning remains coherent only while it stays inside shared homepage composition and shared-selector defaults.

## Why Runtime Implementation Still Fails Closed

- `Verified`: This boundary defines a future homepage-only contract, not an implementation slice.
- `Inference`: The current canon still does not justify an exact runtime file list, exact schema key scope, exact preset selector wiring, exact homepage template wiring, or exact starter-content seeding mechanics.
- `Inference`: Approving implementation here would skip the next decision point that asks whether preset planning is already locally exhausted at the contract-definition edge or whether any later implementation-admission step is honest at all.

## Next Approved Planning Step

- `Verified`: The next governed unit after this boundary should remain docs-only and author one **Phase 4 post-homepage-first preset-pack implementation-boundary frontier review**.
- `Inference`: That frontier review should determine:
  - whether homepage-first preset-pack planning is now locally exhausted at the docs-only contract-definition edge
  - whether any later preset-pack implementation-admission proposal is justified at all
  - whether the repo should remain paused rather than infer a runtime slice from this boundary
  - whether blocked collection and PDP domains should remain explicitly deferred unless fresh dedicated evidence appears

## Why The Other Options Are Rejected

- `Verified`: Reject Option B because this unit must fail closed on runtime authorization and cannot convert contract definition into implementation approval.
- `Verified`: Reject Option C because the current evidence does not justify collection-template, PDP-template, collection-behavior, or PDP-support-behavior consumption.
- `Verified`: Reject Option D because the roadmap plus `UNIT-137` and `UNIT-138` now provide enough evidence to state one exact homepage-only preset contract even though implementation remains blocked.

## What This Boundary Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No exact runtime file list.
- `Verified`: No exact schema contract.
- `Verified`: No exact homepage template or settings-surface selection.
- `Verified`: No collection or PDP template defaults.
- `Verified`: No companion-family reopening.
- `Verified`: No preset-pack implementation.
- `Verified`: No broader commerce, motion/media, JS, or navigation follow-on.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Keep the next step preset-pack-specific and fail-closed on runtime implementation.
- `Verified`: Require the next step to decide whether homepage-first preset-pack planning is exhausted or whether one later implementation-admission proposal is honestly justified.
- `Verified`: Require the next step to keep the boundary homepage-only and selector-based.
- `Verified`: Require the next step to keep collection-template starter structure and PDP-template starter structure explicit placeholders.
- `Verified`: Require the next step to restate blocked surfaces clearly: `collection companion`, `PDP companion`, `collection behavior`, `PDP support behavior`, collection-template rewrites, archive controls, navigation-system behavior, collection-banner authority, support-strip implementation admission, collection-derived strip content, curated-explainer/editorial drift, broader motion/media scope, preset-pack implementation, wider JS, heavier commerce, and `product-card behavior` reuse.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next step authorizes runtime edits directly.
- `Verified`: Stop if it treats deferred `collection companion` or `PDP companion` work as implicitly resolved or consumed.
- `Verified`: Stop if it fills `collection behavior`, `PDP support behavior`, `collection-template starter structure`, or `PDP-template starter structure` as if companion-family evidence already existed.
- `Verified`: Stop if it normalizes support-strip implementation admission, collection-derived strip content, collection-template rewrites, archive controls, navigation-system behavior, collection-banner authority, curated-explainer/editorial drift, broader motion/media scope, preset-pack implementation, wider JS, heavier commerce, or `product-card behavior` reuse as implicit next steps.
- `Verified`: Stop if it infers an implementation slice from this boundary without fresh evidence.

## Readiness Verdict

- `Verified`: READY as a docs-only exact homepage-only preset-pack runtime-facing boundary package.
- `Verified`: NOT READY for runtime implementation.
- `Inference`: The intended next execution target is `UNIT-140`, a docs-only Phase 4 post-homepage-first preset-pack implementation-boundary frontier review.
