# Phase 4 Homepage-First Preset-Pack Implementation-Boundary Proposal Assumptions v1

- `Verified`: `docs/architecture/stonev2-build-roadmap.md` already fixes the five-preset lineup and the homepage-oriented preset fields that Phase 4 must eventually define.
- `Verified`: `UNIT-137` already concluded that only resolved shared families and shared settings may inform preset planning now, while collection-dependent and PDP-dependent assumptions must remain blocked or placeholder-bound.
- `Verified`: `UNIT-138` already concluded that the five roadmap presets can be expressed coherently through homepage-first shared defaults only, while `collection behavior`, `PDP support behavior`, `collection-template starter structure`, and `PDP-template starter structure` remain blocked or placeholder-bound.
- `Inference`: Because the roadmap and shared-defaults verdict already isolate homepage sequence, selector posture, homepage-safe emphasis, and homepage starter structure as the admitted preset surface, the remaining decision is whether that homepage-only preset layer is specific enough to name as a runtime-facing contract without granting implementation approval.
