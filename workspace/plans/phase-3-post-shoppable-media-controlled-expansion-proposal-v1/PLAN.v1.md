# Phase 3 Post Shoppable Media Controlled Expansion Proposal v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to recommend the next honest higher-boundary planning problem after the bounded shoppable-media runway was judged locally exhausted.

## Compared Options

### Option A, reopen motion or broader media-system planning

- `Inference`: Try to keep continuity with the recently verified motion-story proof by opening a larger media or motion planning lane.
- `Inference`: This is not honest now because the remaining motion ideas sit inside the already rejected zone of autoplay, looping, custom controls, broader JS, or heavier media-system behavior.

### Option B, open a Phase 3 companion-systems controlled expansion planning track

- `Inference`: Shift the next higher-boundary planning problem toward `collection companion` and `PDP companion` systems, which are still called out explicitly in `docs/architecture/stonev2-build-roadmap.md` Phase 3.
- `Inference`: Keep the immediate next unit docs-only so Stone V2 can decide whether one broader companion-system boundary is justified, which family should go first, and which template-coupled risks must remain blocked.

### Option C, jump directly to Phase 4 preset-pack planning

- `Inference`: This is plausible because preset packs are the next roadmap phase, but it would skip unresolved Phase 3 family work.
- `Inference`: Preset sequencing should consume stabilized family decisions rather than substitute for them.

### Option D, stop family expansion entirely and pause after shoppable media

- `Inference`: This is the safest fallback.
- `Inference`: It is not the strongest evidence-based choice because the roadmap and family ledger still name unresolved companion systems, and earlier bounded collection/PDP proofs show there is still a planning problem worth evaluating without reopening runtime.

## Recommendation

- `Verified`: **Recommend Option B.**
- `Inference`: The next honest higher-boundary problem is no longer inside shoppable media. It is whether Stone V2 should reopen `collection companion` and `PDP companion` work as a broader Phase 3 companion-systems track, and in what order, while keeping runtime authorization fail-closed.

## Proposed Next Planning Problem

- `Verified`: The next governed unit after this proposal should remain docs-only and author one **Phase 3 companion-systems controlled expansion proposal**.
- `Inference`: That next package should determine:
  - whether `collection companion` or `PDP companion` is the correct first reopened family under broader Phase 3 planning
  - whether the companion-system problem should remain sequenced family-by-family or require one shared broader boundary first
  - which template-coupled surfaces must remain disallowed unless a later boundary makes them explicit
  - whether any future companion-system boundary belongs in Phase 3 at all, or whether the repo should instead pause and defer to Phase 4 preset work

## Why This Is The Best Next Higher-Boundary Problem

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` still lists `collection and PDP companion systems` inside Phase 3, while Phase 4 preset-pack work comes later.
2. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` treats `collection companion` and `PDP companion` as distinct families rather than extensions of homepage-first storytelling.
3. `Inference`: `UNIT-071` through `UNIT-076` proved only bounded Phase 2 object-aware companion slices, not a broader Phase 3 system decision, so a docs-only planning step is still required before any companion runtime work could be justified.
4. `Inference`: Reopening motion, broader media, or heavier commerce now would directly contradict the verified fail-closed verdict from `UNIT-125`.

## What This Proposal Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No edits under `project/`.
- `Verified`: No collection-template or product-template runtime work.
- `Verified`: No broader motion-system scope, autoplay, looping, custom controls, wider JS, or heavier commerce behavior.
- `Verified`: No preset-pack implementation.
- `Verified`: No renewed automatic shoppable-media runtime expansion.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Require the next step to compare `collection companion`, `PDP companion`, `pause`, and `preset-pack deferral` options explicitly.
- `Verified`: Require the next step to keep runtime authorization blocked unless a later boundary is made explicit with exact allowed and disallowed surfaces.
- `Verified`: Require the next step to record why Phase 2 companion proofs are or are not enough evidence for broader Phase 3 family reopening.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next planning step authorizes runtime edits directly.
- `Verified`: Stop if it treats collection-template rewrites, product-template rewrites, recommendation logic, quick-add, quick-view, buy-box behavior, or broader commerce actions as implicitly approved.
- `Verified`: Stop if it tries to reopen shoppable-media runtime work through vague companion or campaign wording.
- `Verified`: Stop if it jumps to preset-pack implementation without explicitly recording why unresolved Phase 3 companion-system questions no longer matter.

## Readiness Verdict

- `Verified`: READY as a docs-only proposal.
- `Inference`: The next governed unit after this proposal should remain docs-only and author a Phase 3 companion-systems controlled expansion proposal.
