# Phase 2 Minimal Trust Stat Strip Assumptions v1

## Confirmed Assumptions

- `Verified`: `UNIT-047` completed and advanced the repo back to a documentation-only planning boundary.
- `Verified`: The next approved planning surface remains limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: The trust-and-support family already includes FAQ, reassurance, help prompt, logo proof, and testimonial proof slices.
- `Verified`: The section-family ledger explicitly names `stat` as a reusable trust-and-support block family.

## Working Assumptions

- `Inference`: A restrained stat strip is the next smallest trust-and-support proof that stays materially distinct from the verified testimonial, logo-rail, reassurance-lane, and FAQ slices.
- `Inference`: The stat strip can stay static and non-JS while still proving another reusable trust-support pattern.
- `Inference`: The shared section shell remains sufficient for this proof and should not require any snippet changes.
- `Inference`: Bilingual locale additions for a stat strip can remain structurally aligned without expanding the editor vocabulary beyond the existing normalized patterns.

## Assumptions To Re-check During Implementation

- `Inference`: Stat-card styling can remain bounded to `project/assets/stone-base.css` without reopening global token work.
- `Inference`: The section can remain proof-oriented without drifting into guarantees, ratings, or claim inflation.
- `Inference`: The future implementation can use one neutral host template only.

## Failure Triggers

- `Verified`: If the future slice requires counter animation, charting, badges, icons, or any JS-driven value treatment, this plan should be treated as invalid.
- `Verified`: If the future slice requires protected-surface edits, `project/blocks/`, a new snippet, a second section, or a second template, this plan should be treated as invalid.
- `Verified`: If the section starts behaving like a guarantee strip, a comparison grid, or a ratings surface, this plan should be treated as invalid.
