# Phase 2 Minimal Trust Stat Strip Plan v1

## Status

- `Verified`: This package is a documentation-only planning artifact.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to bound the next safe Phase 2 runtime slice after the verified minimal trust testimonial slice.

## Objective

- `Verified`: Define the next smallest useful runtime slice that extends the verified trust-and-support proof chain beyond FAQ, reassurance-lane, trust-help-prompt, trust-logo-rail, and trust-testimonial coverage without broadening into homepage composition, preset work, collection work, PDP work, blog or editorial work, ratings systems, guarantee systems, review aggregates, counters, or contact-form implementation.
- `Verified`: Keep the next slice limited to:
  - one bounded base-layer CSS extension in `project/assets/stone-base.css`
  - one new trust-and-support section file in `project/sections/stone-trust-stat-strip.liquid`
  - one neutral alternate page template in `project/templates/page.stone-runtime-trust-stat-strip.json`
  - paired locale updates in `project/locales/he.default.schema.json` and `project/locales/en.schema.json`
- `Verified`: Keep shared-shell edits, runtime-JS edits, global settings edits, snippet work, block-file work, second-section work, and second-template work out of scope.

## Canonical Inputs

- `Verified`: `docs/architecture/stonev2-repo-basis.md`
- `Verified`: `docs/architecture/stonev2-product-thesis.md`
- `docs/architecture/stonev2-section-block-platform.md`
- `docs/architecture/stonev2-settings-bilingual-editor.md`
- `docs/architecture/stonev2-build-roadmap.md`
- `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md`
- `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md`
- `workspace/specs/STONEV2_BILINGUAL_SCHEMA_STRATEGY.md`
- `Verified`: the completed Phase 2 plan packages already present under `workspace/plans/`
- `project/assets/stone-base.css`
- `project/snippets/stone-section-shell.liquid`
- `state/ORCHESTRATION_STATE.md`
- `state/NEXT_ACTION.md`

## Next Bounded Runtime Slice

- `Verified`: The next bounded runtime slice is a **minimal trust stat strip** slice.
- `Inference`: This slice should prove one bounded trust-and-support behavior by rendering restrained static trust metrics with optional short intro copy and short supporting labels only.
- `Inference`: The section should stay bounded by supporting a small static stat set without introducing animated counters, charts, badges, icons, star ratings, review aggregates, guarantees, drawers, popovers, or JavaScript-driven interaction.

## Planned Future Runtime File Scope

| Surface | Type | Planned role |
| --- | --- | --- |
| `project/assets/stone-base.css` | `Inference` | Extend the verified base layer with neutral trust-stat-strip primitives only, such as stat-card spacing, restrained value treatment, and simple layout rules. |
| `project/sections/stone-trust-stat-strip.liquid` | `Inference` | Implement one bounded trust-and-support section that reuses the verified shared section shell and renders a restrained static stat strip only. |
| `project/templates/page.stone-runtime-trust-stat-strip.json` | `Inference` | Add one neutral alternate page template that mounts only the new trust stat strip section for isolated runtime validation. |
| `project/locales/he.default.schema.json` | `Inference` | Add Hebrew section-name, setting-label, block-label, option-label, and help-text coverage for the new trust stat strip section. |
| `project/locales/en.schema.json` | `Inference` | Add English schema coverage structurally aligned with the Hebrew additions for the same section. |

## Explicit Runtime Exclusions

- `Verified`: No edits to `project/layout/theme.liquid` in this slice.
- `Verified`: No edits to `project/assets/stone-runtime.js` in this slice.
- `Verified`: No edits to `project/config/settings_schema.json` in this slice.
- `Verified`: No edits to `project/snippets/stone-section-shell.liquid` in this slice.
- `Verified`: No files under `project/blocks/` in this slice.
- `Verified`: No new snippet file in this slice.
- `Verified`: No second section file in this slice.
- `Verified`: No second template file in this slice.
- `Verified`: No homepage work, no preset work, no collection work, no PDP work, and no blog or editorial work in this slice.
- `Verified`: No hero, story, merchandising, shoppable-media, collection-companion, or PDP-companion family work in this slice.
- `Verified`: No runtime JS, no counter animation, no chart behavior, no drawer behavior, no modal behavior, and no contact-form behavior in this slice.
- `Verified`: No stars, no ratings bands, no review aggregates, no guarantee framing, no badges, no icons, no avatar-led UGC treatment, and no campaign framing in this slice.

## Builder

- `Inference`: The verified shell, verified section shell, verified utility-family proofs, verified trust FAQ proof, verified reassurance-lane proof, verified trust-help-prompt proof, verified trust-logo-rail proof, and verified trust-testimonial proof already establish neutral hosting, disclosure, reassurance grouping, native-link help behavior, brand-proof presentation, and quote-based social proof.
- `Inference`: The next smallest unproven step is a restrained trust stat strip that validates metric-led proof without reopening ratings, guarantees, or animation systems.
- `Inference`: The future runtime slice should proceed in this order:
  1. Extend `project/assets/stone-base.css` with neutral trust-stat-strip primitives that support restrained value emphasis and simple strip layout without broader family styling.
  2. Create `project/sections/stone-trust-stat-strip.liquid` as one trust-and-support section that consumes the existing shared section shell unchanged and renders a bounded static stat strip only.
  3. Create `project/templates/page.stone-runtime-trust-stat-strip.json` as one neutral host that mounts only the new section for isolated validation.
  4. Update `project/locales/he.default.schema.json` and `project/locales/en.schema.json` together so the new section stays bilingual and terminology-aligned.
- `Inference`: This path is minimal because it proves another distinct trust-and-support behavior without reopening the shared shell, adding runtime JS, adding block files, adding snippets, or composing multiple sections into a page-level experience.

## Adversarial Reviewer

- `Inference`: The main failure mode is semantic inflation, where a restrained stat strip turns into a ratings band, guarantee strip, or performance-claim system.
- `Inference`: Another failure mode is interaction drift if implementation starts requiring animated counters, progress bars, or chart behavior to feel complete.
- `Inference`: A third failure mode is presentation drift if the slice starts depending on icons, badges, avatars, or campaign framing to justify itself.
- `Inference`: A fourth failure mode is protected-surface backtracking if the slice starts requiring edits to `project/snippets/stone-section-shell.liquid`, `project/layout/theme.liquid`, `project/assets/stone-runtime.js`, or `project/config/settings_schema.json`.
- `Inference`: A fifth failure mode is scope creep if execution begins demanding files under `project/blocks/`, helper snippets, more than one section, or more than one template.

## Reconciliation

- `Verified`: The approved next slice should remain restricted to the five planned runtime files listed above.
- `Verified`: The future section must stay in the trust-and-support family and must not become a disguised ratings band, a guarantee strip, a comparison grid, or an animated metrics module.
- `Verified`: The existing shared section shell must be consumed unchanged; if it is insufficient for the stat-strip proof, stop and replan instead of reopening it inside this unit.
- `Inference`: The safest proof is one restrained static strip of stat items with optional short intro copy and plain supporting labels only, because that validates another heavier trust-and-support behavior without introducing runtime JS.
- `Inference`: Avoiding counters, icons, badges, guarantees, ratings, and review semantics keeps the slice aligned with anti-chaos rules while preserving boundedness.

## Why This Slice Is Next

- `Verified`: The verified trust FAQ, reassurance lane, trust help prompt, trust logo rail, and trust testimonial slices already proved five distinct trust-and-support behaviors: static disclosure, short reassurance grouping, native-link help guidance, static brand-proof presentation, and quote-led trust signals.
- `Inference`: The next smallest still-unproven step in the trust-and-support family is a restrained stat-led proof that validates concise metric presentation.
- `Inference`: This is heavier than testimonials, but still safer than guarantees or animated counters because it can remain static, non-JS, and layout-light.

## Rejected Broader Alternatives

- `Verified`: Homepage work is rejected because it would force composition and hierarchy decisions before another reusable trust/support proof is established.
- `Verified`: Preset work is rejected because presets belong after more reusable family behaviors exist and stay stable.
- `Verified`: Collection work is rejected because it introduces collection-specific navigation and merchandising assumptions.
- `Verified`: PDP work is rejected because it introduces product-detail and support-assumption coupling.
- `Verified`: Blog or editorial work is rejected because it introduces storytelling semantics outside the trust/support proof.
- `Verified`: Guarantee strips are rejected because they introduce stronger promise semantics than the bounded proof target.
- `Verified`: Ratings bands and review aggregates are rejected because they imply broader data and semantics than a bounded stat proof.
- `Verified`: Animated counters, charts, progress bars, or badge systems are rejected because they introduce motion systems, semantic inflation, or likely JS pressure.
- `Verified`: Editing `project/layout/theme.liquid`, `project/assets/stone-runtime.js`, `project/config/settings_schema.json`, or `project/snippets/stone-section-shell.liquid` is rejected because those are protected surfaces for this slice.
- `Verified`: Any `project/blocks/` file, any new snippet file, any second section file, any second template file, and any JS-driven behavior are rejected because they broaden the slice beyond one bounded sixth trust proof.

## Non-Goals

- `Verified`: Implement a ratings band, review aggregate, guarantee strip, comparison grid, chart, progress meter, or contact flow.
- `Verified`: Add new runtime JS behavior, counters, motion logic, drawers, modals, tabs, sliders, popovers, or cart behavior.
- `Verified`: Add new global settings groups or change the foundation settings vocabulary.
- `Verified`: Build homepage, collection, PDP, blog, article, or preset composition.
- `Verified`: Deliver polished analytics or claim systems beyond neutral trust-stat-strip primitives.

## Expected Outputs Of The Future Runtime Slice

- `Inference`: One new trust-and-support section file that proves a bounded static stat strip within the existing shared shell.
- `Inference`: One neutral alternate page template that mounts only that section for isolated validation.
- `Inference`: One bounded extension of `project/assets/stone-base.css` for neutral trust-stat-strip primitives.
- `Inference`: One paired Hebrew and English locale update that keeps the section and its schema vocabulary bilingual.
- `Verified`: No runtime files outside the explicitly planned five-file scope.

## What Would Invalidate The Slice And Require Replanning

- `Verified`: Replan if implementation needs any runtime file outside the planned five-file set plus normal state and handoff updates.
- `Verified`: Replan if the future section needs edits to `project/snippets/stone-section-shell.liquid`, `project/layout/theme.liquid`, `project/assets/stone-runtime.js`, or `project/config/settings_schema.json`.
- `Verified`: Replan if the trust stat strip needs `project/blocks/` files, a new snippet, a second section, a second template, custom JS, counters, charts, badges, icons, ratings, review aggregates, guarantees, or motion systems.
- `Verified`: Replan if the section cannot stay static and restrained without broadening scope.
- `Verified`: Replan if Hebrew and English locale coverage cannot remain structurally aligned in the same bounded change set.

## Stop Condition

- `Verified`: Stop if the future implementation needs any file outside `project/assets/stone-base.css`, `project/sections/stone-trust-stat-strip.liquid`, `project/templates/page.stone-runtime-trust-stat-strip.json`, `project/locales/he.default.schema.json`, and `project/locales/en.schema.json`.
- `Verified`: Stop if the slice starts editing `project/layout/theme.liquid`, `project/assets/stone-runtime.js`, `project/config/settings_schema.json`, or `project/snippets/stone-section-shell.liquid`.
- `Verified`: Stop if the slice starts implementing homepage composition, preset wiring, collection surfaces, PDP surfaces, blog or editorial surfaces, or hero, story, merchandising, shoppable-media, or contact-form behavior.
- `Verified`: Stop if the slice introduces `project/blocks/`, any new snippet file, any second new section file, any second new template file, any custom JS-driven behavior, any counters, any chart behavior, any badges, any icons, any stars, any ratings treatment, any review aggregate semantics, any guarantee framing, or any animated value treatment.
- `Verified`: Stop if the slice stops being a bounded trust stat strip proof or stops fitting inside the approved five runtime files.

## Relationship To Verified Runtime State

- `Verified`: `project/snippets/stone-section-shell.liquid` already provides the neutral wrapper contract that the new section should consume unchanged.
- `Verified`: The verified trust FAQ slice proved a first trust-and-support behavior through disclosure.
- `Verified`: The verified reassurance lane slice proved a second trust-and-support behavior through restrained cue grouping.
- `Verified`: The verified trust help prompt slice proved a third trust-and-support behavior through native-link help guidance.
- `Verified`: The verified trust logo rail slice proved a fourth trust-and-support behavior through static brand-proof presentation.
- `Verified`: The verified trust testimonial slice proved a fifth trust-and-support behavior through quote-led static proof.
- `Verified`: `project/assets/stone-base.css` is already the correct globally loaded substrate surface for neutral section-family layout rules.
- `Verified`: The section-family ledger explicitly lists `stat` among the reusable trust-and-support block families.
- `Verified`: The bilingual schema strategy still requires any new section strings to land in Hebrew and English together.

## Readiness Verdict

- `Verified`: READY for one separately governed and tightly bounded planning-to-implementation handoff.
- `Verified`: The intended next execution target is `UNIT-049`, limited to the exact future file scope above.
