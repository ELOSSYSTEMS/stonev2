# Phase 2 Minimal Trust Stat Strip Risks v1

## Primary Risks

1. `Inference`: Semantic drift could turn a restrained stat strip into guarantee messaging, ratings logic, or unsupported claim framing.
2. `Inference`: Interaction drift could push the slice toward animated counters, progress meters, or other JS-backed value treatment.
3. `Inference`: Presentation drift could push the section toward icons, badges, comparison layouts, or broader campaign styling.
4. `Inference`: Structural drift could turn one bounded proof into a second section, a second template, or a new snippet dependency.
5. `Inference`: Locale drift could create schema mismatches between Hebrew and English.

## Mitigations

- `Verified`: Keep the planned runtime surface restricted to the same five-file pattern used by the recent trust-family slices.
- `Verified`: Reuse the shared section shell unchanged.
- `Verified`: Keep the future template neutral and isolated.
- `Verified`: Fail closed if implementation needs JS, counters, icons, badges, guarantee language, or claim-heavy semantics.
- `Verified`: Update locale files together and verify structural parity before accepting the slice.

## Replanning Threshold

- `Verified`: Replan immediately if the stat strip cannot remain a static, plain, trust-oriented metric proof inside the approved five runtime files.
