# Phase 4 Collection Host And Behavior Reopen Evidence v1

## Status

- `Verified`: This package is docs-only.
- `Verified`: It is pre-authored and conditional on the homepage and shell lanes being completed cleanly enough to justify a collection reopen.
- `Verified`: It does not authorize runtime edits by itself.

## Scope

- `Verified`: Reassess collection host surfaces, product-grid behavior, filter/sort/paginate behavior, and collection companion fit.
- `Verified`: Use the corpus and prior `collection support strip` decisions without pretending they already authorize runtime.
- `Verified`: Decide whether one exact collection reopen lane is justified and how narrow it must be.

## Builder

- `Inference`: The minimal honest collection reopen is host-and-behavior first, not full companion-family breadth.
- `Inference`: The package should compare:
  1. keep collection blocked
  2. reopen a bounded collection host/product-grid lane
  3. reopen broader collection companion behavior
- `Inference`: The strongest likely outcome is a narrow collection host/product-grid lane with companion assumptions still explicit.

## Adversarial reviewer

- `Inference`: The collection corpus is large enough to encourage overreach into filters, archive controls, navigation aids, banners, explainers, and recommendation logic all at once.
- `Inference`: Prior Phase 3 work explicitly rejected broad collection drift, so this package fails if it quietly reintroduces those surfaces without a fresh narrower justification.
- `Inference`: Product-card reuse, archive controls, and navigation behavior remain especially risky drift zones.

## Reconciliation

- `Verified`: Reopen collection only if the package can keep host behavior distinct from broader companion-family and archive drift.
- `Verified`: Keep collection banner, navigation-aid, archive-control, and broader editorial drift blocked unless separately re-admitted.
- `Inference`: The expected result is one later bounded collection host/behavior boundary, not a direct build authorization.

## Acceptance Criteria

1. `Verified`: Reassess collection surfaces against prior Phase 3 blocked surfaces explicitly.
2. `Verified`: Record which collection behaviors are admissible now versus still blocked.
3. `Verified`: Keep product-card reuse authority explicit rather than implied.
4. `Verified`: Keep broader collection companion breadth blocked unless separately justified.

## Intended Next Step If This Unit Passes

- `Inference`: Route to one later bounded collection host/behavior boundary package.

## Readiness Verdict

- `Verified`: READY only after homepage and shell lanes stop being the stronger unresolved dependency.
