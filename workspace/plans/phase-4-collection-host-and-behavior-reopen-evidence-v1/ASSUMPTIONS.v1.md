# Phase 4 Collection Host And Behavior Reopen Evidence Assumptions v1

- `Verified`: Prior collection companion work through `UNIT-134` kept broader collection reopening fail-closed.
- `Verified`: Homepage and shell/navigation lanes are now verified enough to remove them as stronger unresolved dependencies.
- `Verified`: Current runtime has product-card and collection-card primitives but no canonical collection host.
- `Inference`: A later collection reopen must start with host and product-grid boundary definition before broader companion-family breadth.
- `Inference`: Native Shopify collection data and existing product-card semantics are enough to justify a boundary package, not direct implementation.
