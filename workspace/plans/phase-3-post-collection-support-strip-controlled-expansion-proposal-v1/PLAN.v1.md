# Phase 3 Post Collection Support Strip Controlled Expansion Proposal v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to determine the next honest higher-boundary planning problem, if any, after the `collection support strip` lane was judged locally exhausted at the docs-only contract-definition edge.

## Compared Options

### Option A, remain paused and approve no further planning step yet

- `Inference`: This is the safest immediate fallback because `UNIT-133` already concluded that the strongest narrowed `collection companion` lane is exhausted and does not justify implementation admission.
- `Inference`: It is not the strongest next planning move if continuation is still desired, because it leaves the remaining roadmap-ordering question implicit instead of stating whether unresolved companion-family work should now stay paused inside Phase 3 or be explicitly deferred behind later preset sequencing.

### Option B, reopen a broader `collection companion` review

- `Inference`: This would revisit the same broader family boundary that `UNIT-128` and `UNIT-129` already narrowed successfully.
- `Inference`: It would overread the canon because the strongest narrowed lane inside that family was already identified as `collection support strip`, and `UNIT-133` then concluded that even this strongest lane is exhausted at the docs-only contract-definition edge.
- `Inference`: Returning to a broader `collection companion` review now would mostly relitigate weaker banner, curated-explainer, or navigation-adjacent routes that were already deferred or blocked when support strip was selected as the best-evidenced candidate.

### Option C, reopen a broader companion-family review

- `Inference`: This would revisit the higher-boundary `collection companion` versus `PDP companion` question from `UNIT-127`.
- `Inference`: It is not justified now because `UNIT-127` explicitly chose collection-first reopening as the safer family path, and the collection-first lane has now exhausted its strongest candidate without earning any later implementation-admission step.
- `Inference`: Reopening broader companion-family review here would risk treating `PDP companion`, shared companion language, or another family-wide reopening as newly justified even though the safer collection-first lane did not produce stronger runtime-admission evidence.

### Option D, authorize one docs-only roadmap-deferral proposal instead of another family reopening

- `Inference`: This keeps the repo fail-closed on runtime and companion-family reopening while still addressing the remaining higher-boundary planning question honestly.
- `Inference`: The unresolved question is no longer which `collection companion` lane should go next. It is whether the unresolved companion-family backlog should now remain paused and be explicitly deferred behind Phase 4 preset-pack planning, or whether the roadmap still requires another Phase 3 companion-family reopening later.
- `Inference`: This is the smallest evidence-preserving continuation because it turns the open problem into roadmap sequencing rather than pretending another companion-family lane is ready.

## Recommendation

- `Verified`: **Recommend Option D.**
- `Inference`: No later family reopening is justified from the current evidence. If work continues, the next honest planning problem is one docs-only **Phase 3 companion roadmap deferral proposal** rather than another broader `collection companion` review, another broader companion-family review, or direct runtime work.

## Exact Controlled-Expansion Verdict

- `Verified`: `UNIT-133` exhausted the strongest narrowed `collection companion` lane at the docs-only contract-definition edge.
- `Verified`: No further `collection companion` family reopening is justified from the current evidence.
- `Verified`: No broader companion-family reopening is justified from the current evidence.
- `Inference`: The remaining honest question is roadmap ordering: whether unresolved companion-family work should now be kept paused and explicitly deferred behind later preset-pack planning rather than forcing another Phase 3 family reopening.
- `Verified`: This verdict remains docs-only and does not authorize runtime work directly.

## Why Another Family Reopening Is Not Honest Now

1. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` names `collection support strip`, `collection banner`, and curated collection explainer modes inside the `collection companion` family, but `UNIT-129` already concluded that `collection support strip` was the strongest candidate lane among them.
2. `Verified`: `UNIT-130` through `UNIT-132` then narrowed that strongest lane to one exact docs-only support-strip contract while repeatedly keeping runtime implementation, runtime file scope, schema scope, and host-surface selection fail-closed.
3. `Verified`: `UNIT-133` concluded that the support-strip lane is locally exhausted at the contract-definition edge and that no implementation-admission proposal is justified from the current evidence.
4. `Inference`: If the strongest collection-companion lane cannot honestly move beyond docs-only contract definition, then weaker adjacent collection-companion routes such as banner, curated explainer, or navigation-adjacent review are not honestly stronger next moves.
5. `Verified`: `UNIT-127` already established that companion-family reopening had to stay collection-first and fail-closed on `PDP companion`.
6. `Inference`: If the safer collection-first reopening did not yield enough evidence for another family-level advance, then reopening broader companion-family review now would overread the canon and would risk smuggling `PDP companion`, shared companion language, or broader commerce pressure back into scope.

## Why Roadmap Deferral Is The Best Next Higher-Boundary Problem

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` still places `collection and PDP companion systems` in Phase 3 before preset-pack implementation.
2. `Inference`: The repo has now tested the collection-first reopening path as far as the current canon honestly supports, and that path stopped at a docs-only contract-definition edge rather than producing a justified runtime-admission problem.
3. `Inference`: That leaves an unresolved sequencing question that the roadmap still has not answered explicitly: whether unresolved companion-family work should remain paused inside Phase 3 or be deferred behind Phase 4 preset-pack planning until stronger evidence appears.
4. `Inference`: Addressing that sequencing question is more honest than forcing another family reopening, because it acknowledges that the open issue is no longer lane selection inside companion families but roadmap ordering across phases.

## Why The Other Options Are Rejected

- `Verified`: Reject Option A because pure pause is the safest fallback but leaves the roadmap-ordering question implicit instead of resolving whether Phase 3 companion-family work should now be explicitly deferred.
- `Verified`: Reject Option B because a broader `collection companion` review would simply relitigate a family whose strongest narrowed lane has already been exhausted without supporting implementation admission.
- `Verified`: Reject Option C because broader companion-family review would reopen the more commerce-adjacent `PDP companion` and shared companion framing despite the collection-first lane failing to justify a further reopening.

## What This Proposal Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No edits under `project/`.
- `Verified`: No support-strip implementation-admission proposal.
- `Verified`: No collection-derived strip content.
- `Verified`: No collection-template rewrites.
- `Verified`: No collection archive controls or navigation-system behavior.
- `Verified`: No collection-banner authority or curated-explainer/editorial drift.
- `Verified`: No direct or implied `PDP companion` reopening.
- `Verified`: No broader motion/media continuation.
- `Verified`: No preset-pack implementation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce continuation.
- `Verified`: No `product-card behavior` reuse as an implied next step.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Require the next step to stay at the roadmap-sequencing level rather than reopening `collection companion` or `PDP companion` directly.
- `Verified`: Require the next step to compare at least explicit pause versus explicit roadmap deferral semantics for unresolved companion-family work.
- `Verified`: Require the next step to keep preset-pack implementation fail-closed while evaluating whether preset sequencing should absorb unresolved companion-family work.
- `Verified`: Require the next step to fail closed if it tries to turn roadmap deferral into implicit runtime authorization or a disguised family reopening.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next planning step authorizes runtime edits directly.
- `Verified`: Stop if it reopens broader `collection companion` or broader companion-family review as if those were already justified by `UNIT-134`.
- `Verified`: Stop if it normalizes support-strip implementation admission, collection-derived content, collection-template rewrites, archive controls, navigation-system behavior, collection-banner authority, curated-explainer/editorial drift, `PDP companion`, broader motion/media, preset-pack implementation, wider JS, heavier commerce, or `product-card behavior` reuse as approved follow-ons.
- `Verified`: Stop if it treats roadmap deferral as implicit permission to skip unresolved Phase 3 companion-family questions without recording the sequencing tradeoff explicitly.

## Next Approved Planning Step

- `Verified`: The next governed unit after this proposal should remain docs-only and author one **Phase 3 companion roadmap deferral proposal**.
- `Inference`: That proposal should determine:
  - whether unresolved `collection companion` and `PDP companion` work should remain paused inside Phase 3
  - whether the roadmap should explicitly defer unresolved companion-family work behind Phase 4 preset-pack planning
  - which Phase 3 family assumptions must remain blocked if preset sequencing moves ahead first
  - what fresh evidence would be required before any later companion-family reopening could become honest again

## Readiness Verdict

- `Verified`: READY as a docs-only controlled expansion proposal.
- `Inference`: The next governed unit after this proposal should remain docs-only and author a Phase 3 companion roadmap deferral proposal.
