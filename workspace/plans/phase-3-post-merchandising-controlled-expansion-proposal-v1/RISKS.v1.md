# Phase 3 Post Merchandising Controlled Expansion Proposal Risks v1

1. `Inference`: It would be easy to reopen thin merchandising runtime slices instead of admitting that the next problem is larger and interaction-aware.
2. `Inference`: A future shoppable-media planning step could drift into premature implementation if JS and protected-surface questions are not handled explicitly.
3. `Verified`: The mitigation is to keep the next step docs-only, require exact allowed/disallowed surfaces, and fail closed on any attempt to smuggle runtime implementation into the boundary definition.
