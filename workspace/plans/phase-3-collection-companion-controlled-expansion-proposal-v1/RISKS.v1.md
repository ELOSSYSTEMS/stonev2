# Phase 3 Collection Companion Controlled Expansion Proposal Risks v1

1. `Inference`: The proposal could still smuggle collection-template rewrites or hero/opening authority back in if `support strip` is described too loosely as banner framing.
2. `Inference`: The shared `collection behavior` and `product-card behavior` vocabularies could be misread as approval for broader collection systems, richer merchandising reuse, or heavier commerce.
3. `Inference`: `Navigation aids` could slip back into scope through vague support-strip language and reopen archive controls indirectly.
4. `Inference`: The proposal could also overreach by treating curated explainers or PDP companion reopening as implicit next steps instead of later separate questions.
5. `Verified`: The mitigation is to keep this unit planning-only, nominate only `collection support strip` as the next narrowed docs-only lane, and keep runtime, archive, template-rewrite, editorial-drift, banner-drift, preset-pack, broader JS, and heavier commerce work fail-closed.
