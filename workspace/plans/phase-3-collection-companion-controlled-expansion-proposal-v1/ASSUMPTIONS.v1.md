# Phase 3 Collection Companion Controlled Expansion Proposal Assumptions v1

- `Verified`: `docs/architecture/stonev2-build-roadmap.md` still treats collection and PDP companion systems as unresolved Phase 3 family work that precedes preset-pack implementation.
- `Verified`: `docs/architecture/stonev2-section-block-platform.md` explicitly names both `collection banners` and `collection support strips` inside companion-family coverage.
- `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` keeps `collection companion` distinct from merchandising and `PDP companion`, and explicitly names preset logic for `banner`, `support strip`, and `category explainer` modes.
- `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` includes `support-strip visibility` inside normalized `collection behavior`, which gives one lane-specific signal beyond generic family naming.
- `Verified`: `UNIT-071` through `UNIT-074` proved only one narrow collection-aware runtime slice and explicitly stopped automatic deeper collection-aware continuation.
- `Verified`: `UNIT-128` concluded that the next honest move must still be docs-only and must identify one narrowed collection-companion lane without reopening archive controls, template rewrites, or direct runtime authorization.
- `Inference`: The strongest evidence-backed narrowed lane is therefore `collection support strip`, with banner and curated explainer left for later only if a narrower support-strip boundary proves clean enough first.
