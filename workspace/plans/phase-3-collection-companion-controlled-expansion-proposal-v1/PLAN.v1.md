# Phase 3 Collection Companion Controlled Expansion Proposal v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to determine which narrowed collection-first companion lane, if any, is the next honest planning problem after the broader collection-companion boundary was reopened.

## Compared Options

### Option A, narrow the next planning step to one collection support strip boundary

- `Inference`: `Collection support strip` is the strongest candidate because it is named in the architecture family list, in the section-family ledger preset guidance, and in the settings taxonomy through `support-strip visibility`.
- `Inference`: It can remain an additive collection-page companion wrapper without automatically collapsing into hero-template rewrites, archive controls, or heavier commerce.
- `Inference`: It is also the narrowest candidate that still uses the reopened broader boundary honestly rather than repeating the already-proved collection context card micro-slice.

### Option B, narrow the next planning step to one collection banner boundary

- `Inference`: `Collection banner` is a real family candidate because it appears in the architecture family list and section-family ledger.
- `Inference`: It is not the best next narrowed lane because banner work sits closer to hero/opening systems and collection-template framing, which increases the risk of implicit template rewrites or broader page-opening authority.

### Option C, narrow the next planning step to one curated collection explainer boundary

- `Inference`: `Curated collection explainer` is named in the section-family ledger and is conceptually valid inside the family.
- `Inference`: It is not the strongest next lane because it overlaps more directly with story/editorial composition, richer content structures, and selective homepage reuse, making distinctness weaker than a support-strip wrapper.

### Option D, fail closed and decline to select any narrowed lane yet

- `Inference`: This is the safest fallback.
- `Inference`: It is not the strongest evidence-based choice because the repo now has enough normalized evidence to select one narrower planning target without authorizing runtime work.

## Recommendation

- `Verified`: **Recommend Option A.**
- `Inference`: The next honest narrowed planning problem is one docs-only **collection support strip boundary** package.

## Exact Proposal Verdict

- `Verified`: The reopened broader collection-companion boundary now has enough evidence to narrow to one collection-first candidate lane.
- `Verified`: The only candidate justified strongly enough for the next approved planning step is `collection support strip`.
- `Verified`: This verdict remains docs-only and does not authorize runtime work directly.
- `Inference`: Banner and curated explainer remain plausible later family questions, but they are not the best immediate next lane because their distinctness from hero/opening or story/editorial systems is weaker.

## Why Collection Support Strip Is The Next Honest Narrowed Lane

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` still keeps collection companion work inside unresolved Phase 3 family implementation rather than preset-pack work.
2. `Verified`: `docs/architecture/stonev2-section-block-platform.md` names `collection support strips` explicitly inside `collection and PDP companion families`.
3. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` says the collection-companion family should use presets for `banner`, `support strip`, and `category explainer` modes rather than uncontrolled file sprawl.
4. `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` gives `collection behavior` a normalized `support-strip visibility` concept, which is stronger lane-specific evidence than the generic family list alone.
5. `Verified`: `UNIT-071` through `UNIT-074` already proved one bounded read-only collection-aware support surface and then explicitly blocked automatic deeper continuation, which means the next lane must stay additive, lighter than merchandising, and fail closed on archive behavior.
6. `Inference`: A support strip is the best match for those constraints because it can stay wrapper-like, collection-page-first, and guidance-oriented without depending on hero-template control, richer narrative composition, or navigation/archive semantics.

## Why The Other Options Are Rejected

- `Verified`: Reject Option B because `collection banner` sits too close to hero/opening structure and collection-template framing, increasing template-rewrite risk before a narrower boundary has proved distinctness.
- `Verified`: Reject Option C because `curated collection explainer` risks drifting into story/editorial composition and broader content-density authority instead of staying a restrained collection wrapper.
- `Verified`: Reject Option D because the repo now has enough evidence to choose one narrower docs-only lane without reopening runtime authorization.

## Navigation-Aid Verdict

- `Verified`: `Navigation aids` remain blocked as an immediate narrowed lane.
- `Inference`: The broader-boundary package already identified navigation language as the easiest way to smuggle archive controls, toolbar behavior, filters, sorting, pagination, search, or template-system drift back into scope.

## What This Proposal Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No edits under `project/`.
- `Verified`: No direct collection support strip implementation boundary yet.
- `Verified`: No collection-template rewrites.
- `Verified`: No collection archive controls, including toolbar behavior, filters, sorting, tags, pagination, search, or navigation-system behavior.
- `Verified`: No direct reopening of `PDP companion`.
- `Verified`: No collection banner or curated collection explainer approval by implication.
- `Verified`: No broader motion/media continuation.
- `Verified`: No preset-pack implementation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce, richer product-card behavior, quick-add, quick-view, or recommendation logic.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Require the next step to evaluate one exact `collection support strip` boundary rather than vague collection-companion wording.
- `Verified`: Require the next step to prove distinctness from hero/opening banners, story/editorial explainers, merchandising rails, and archive/navigation systems.
- `Verified`: Require the next step to record exactly which collection-adjacent surfaces remain blocked, especially template rewrites, archive controls, broader JS, and heavier commerce.
- `Verified`: Require the next step to fail closed if support-strip narrowing would still need direct runtime authorization or broader template/system approval.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next planning step authorizes runtime edits directly.
- `Verified`: Stop if it normalizes collection-template rewrites, archive controls, toolbar behavior, filtering, search, pagination, or navigation-system work as part of support-strip scope.
- `Verified`: Stop if it reopens `PDP companion`, banner/hero authority, curated-explainer/editorial authority, broader motion/media, preset-pack implementation, wider JS, or heavier commerce through implied follow-on language.
- `Verified`: Stop if support-strip distinctness cannot be stated without leaning on broader collection behavior or product-card behavior approval.

## Next Approved Planning Step

- `Verified`: The next governed unit after this proposal should remain docs-only and author one **Phase 3 collection support strip boundary package**.
- `Inference`: That boundary should determine:
  - whether one exact collection support strip distinctness case can stay additive and collection-page-first
  - whether any shared `collection behavior` or `product-card behavior` vocabulary may be referenced without becoming implementation approval
  - which collection-template, archive, hero/opening, editorial, and merchandising surfaces remain blocked explicitly
  - whether a later runtime-facing support-strip boundary is justified at all after those questions are answered

## Readiness Verdict

- `Verified`: READY as a docs-only controlled expansion proposal.
- `Inference`: The next governed unit after this proposal should remain docs-only and author a Phase 3 collection support strip boundary package.
