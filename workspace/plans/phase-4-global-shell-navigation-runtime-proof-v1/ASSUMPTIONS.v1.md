# Phase 4 Global Shell And Navigation Runtime Proof Assumptions v1

- `Verified`: The exact runtime boundary is seven files.
- `Verified`: `project/assets/stone-runtime.js` is out of scope.
- `Inference`: Header and footer snippets are the narrowest reusable structure for global shell placement.
- `Inference`: Menu selection settings are enough to prove global navigation without adding collection or PDP behavior.
