# Phase 4 Post-UNIT-141 Corpus Admission And Reopen Decision Assumptions v1

- `Verified`: `shopify_theme_dom/` is a real local corpus in this repo workspace and not a hypothetical future reference set.
- `Verified`: The corpus covers 9 store captures with homepage and cross-surface page evidence, not only a single homepage reference.
- `Verified`: The current `project/` runtime has section and snippet primitives plus 33 `page.stone-runtime*.json` templates, but it has no canonical host templates such as `index.json`, `collection.json`, `product.json`, `blog.json`, `article.json`, `search.json`, `list-collections.json`, or `cart.json`.
- `Verified`: The homepage-only preset composition/defaults contract from `UNIT-139` and `UNIT-140` remains the narrowest admitted preset frontier.
- `Verified`: `collection companion`, `PDP companion`, `collection behavior`, `PDP support behavior`, `collection-template starter structure`, and `PDP-template starter structure` remain explicitly blocked unless this package proves otherwise.
- `Inference`: The corpus broadens the evidence base more than it narrows one exact reopen lane.
- `Inference`: Homepage-first is still the closest future candidate lane, but that is weaker than the evidence required to approve a later unit now.
