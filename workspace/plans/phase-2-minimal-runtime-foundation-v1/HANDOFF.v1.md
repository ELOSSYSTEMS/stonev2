# Phase 2 Minimal Runtime Foundation Handoff v1

## Summary

- `Verified`: The bounded Phase 2 minimal runtime foundation plan package is complete.
- `Verified`: The package limits the first runtime slice to a theme shell, token layer, base shell CSS, lightweight progressive JavaScript entry, minimal settings schema, and paired Hebrew or English schema locale files.

## Why This Is The Right First Runtime Foundation Step

- `Verified`: It creates the smallest runtime substrate that later Phase 2 and Phase 3 work can build on without guessing shell, token, JavaScript, or schema direction.
- `Verified`: It preserves the architecture canon by keeping the implementation Liquid-first, framework-free, web-component-free, RTL-native, Hebrew-first, and bilingual at the schema layer.
- `Inference`: It is safer than starting with section families or homepage work because it sets the base contract without entangling later product surfaces.

## Intended Next Execution Target After Plan Approval

- `Verified`: `UNIT-004` should execute a conditionally approved minimal runtime foundation implementation slice.
- `Inference`: The intended runtime file scope is:
  - `project/layout/theme.liquid`
  - `project/assets/stone-tokens.css`
  - `project/assets/stone-base.css`
  - `project/assets/stone-runtime.js`
  - `project/config/settings_schema.json`
  - `project/locales/he.default.schema.json`
  - `project/locales/en.schema.json`
- `Verified`: No other runtime files should be introduced in that slice.

## Checks Before Runtime Implementation Begins

- `Verified`: Re-read `PLAN.v1.md`, `ASSUMPTIONS.v1.md`, `CHECKS.v1.md`, and `RISKS.v1.md`.
- `Verified`: Confirm `state/NEXT_ACTION.md` still points to one bounded runtime unit with the same file scope.
- `Verified`: Confirm the active approval state remains aligned between `state/ORCHESTRATION_STATE.md` and `state/NEXT_ACTION.md`.
- `Verified`: Confirm no new canon requirement has appeared that would require section-family or preset-level replanning first.
- `Verified`: Run the local harness checks before and after runtime execution state updates.
