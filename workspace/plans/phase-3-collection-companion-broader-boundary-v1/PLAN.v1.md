# Phase 3 Collection Companion Broader Boundary v1

## Status

- `Verified`: This package is documentation-only.
- `Verified`: It does not authorize runtime edits by itself.
- `Verified`: Its purpose is to determine whether one broader `collection companion` boundary is justified as the first reopened Phase 3 companion-family planning step.

## Compared Options

### Option A, define one broader docs-only `collection companion` boundary and continue only to a narrower planning proposal

- `Inference`: Reopen the family at the broader planning level, but keep the immediate output fail-closed and still one step away from runtime-shaped authorization.
- `Inference`: Treat the next honest move as a docs-only **collection-companion controlled expansion proposal** rather than a direct implementation boundary.
- `Inference`: Keep the first reopened scope centered on additive collection-page companion wrappers such as support-strip, banner, or explainer modes, not archive-system behavior.

### Option B, jump directly from `UNIT-127` to a runtime-facing collection-companion implementation boundary

- `Inference`: This would move too quickly from family reopening to exact runtime authorization.
- `Inference`: The family ledger and settings taxonomy show unresolved coupling to collection behavior vocabulary, merchandising reuse, and collection-page semantics that still need one more planning pass.
- `Inference`: A direct runtime boundary here would make it too easy to smuggle collection-template rewrites, archive controls, or heavier commerce assumptions into the approval surface.

### Option C, reopen `collection companion` and `PDP companion` together through shared companion language

- `Inference`: This would blur two distinct families that the ledger keeps separate.
- `Inference`: It would also weaken the collection-first sequencing verdict already established by `UNIT-127`.

### Option D, pause companion-family planning and defer directly to preset-pack sequencing

- `Inference`: This is conservative, but it would force presets to absorb unresolved collection-companion family decisions.
- `Inference`: Phase 3 still names collection and PDP companion systems explicitly before preset-pack implementation.

## Recommendation

- `Verified`: **Recommend Option A.**
- `Inference`: One broader `collection companion` boundary is justified, but only as a fail-closed planning boundary that hands off to one narrower docs-only proposal instead of direct runtime authorization.

## Exact Boundary Verdict

- `Verified`: Stone V2 now has enough evidence to reopen `collection companion` as the first broader companion-family planning problem.
- `Verified`: The reopening is justified only at the docs-only planning level.
- `Inference`: A direct runtime-facing boundary is not yet justified because collection-companion work now touches collection-page semantics, shared collection behavior vocabulary, and selective merchandising reuse more directly than the earlier Phase 2 micro-slice did.
- `Verified`: The next honest move after this boundary is still planning, not implementation.

## Why This Broader Boundary Is Justified

1. `Verified`: `docs/architecture/stonev2-build-roadmap.md` still places `collection and PDP companion systems` in Phase 3 ahead of preset-pack implementation.
2. `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` defines `collection companion` as its own family with collection-page-first use cases such as banner, support strip, curated explainer, and navigation aids.
3. `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` places `collection behavior` and `product-card behavior` inside shared normalized vocabularies, which means broader collection-companion planning now has to account for adjacent platform contracts rather than pretend it is still an isolated micro-slice.
4. `Verified`: `UNIT-071` through `UNIT-073` proved one bounded collection-aware slice successfully, and `UNIT-074` concluded that deeper collection-aware runtime work should not resume automatically.
5. `Verified`: The later Phase 3 merchandising runway established reusable product-card and collection-led merchandising primitives, and the later shoppable-media runway was then frontier-reviewed and exhausted before companion-family planning was reopened.
6. `Inference`: The repo therefore has enough adjacent platform evidence to ask the broader collection-companion question honestly, but not enough to skip the extra fail-closed planning step.

## Exact Allowed Planning Scope Inside This Broader Boundary

- `Verified`: Define whether `collection companion` can reopen as a broader planning track without authorizing runtime work directly.
- `Verified`: Keep the first reopened scope limited to additive collection-page companion wrappers that remain distinct from archive systems.
- `Verified`: Allow planning attention on collection-companion modes such as support-strip, banner, and curated explainer framing.
- `Verified`: Allow planning references to shared merchandising and collection-behavior vocabularies only as dependency context, not as implicit implementation approval.
- `Verified`: Require exact statements about which collection-adjacent surfaces remain blocked.

## Explicitly Disallowed Inside This Broader Boundary

- `Verified`: No direct runtime approval boundary.
- `Verified`: No edits under `project/`.
- `Verified`: No collection-template rewrites.
- `Verified`: No collection archive controls, including toolbar behavior, filters, sorting, tags, pagination, or search.
- `Verified`: No implicit approval for navigation-aid work where it collapses into archive or template-system behavior.
- `Verified`: No shared `collection companion` and `PDP companion` reopening.
- `Verified`: No preset-pack implementation sequencing as a substitute for unresolved family-boundary work.
- `Verified`: No broader motion/media continuation.
- `Verified`: No wider JS scope.
- `Verified`: No heavier commerce, recommendation logic, quick-add, quick-view, or expanded product-card behavior approval.

## Why Direct Runtime Authorization Fails Closed Here

- `Verified`: The family ledger places `collection companion` on collection pages first, which makes template coupling more immediate than it was in the earlier Phase 2 collection-context-card proof.
- `Verified`: The settings taxonomy keeps collection behavior and product-card behavior in shared vocabularies, which means a premature runtime boundary would risk normalizing broader collection systems or heavier commerce through companion language drift.
- `Inference`: The first broader collection-companion question is therefore not "which exact runtime files should change?" but "which collection-first companion mode, if any, can be narrowed honestly without reopening archive systems or template rewrites?"

## Next Approved Planning Step

- `Verified`: The next governed unit after this boundary should remain docs-only and author one **Phase 3 collection-companion controlled expansion proposal**.
- `Inference`: That proposal should determine:
  - whether the first narrowed collection-companion lane should be support-strip, banner, or curated explainer framing
  - whether any navigation-aid concept can remain safely blocked because it drifts into archive controls
  - which shared merchandising or collection-behavior primitives may be referenced without becoming implementation approval
  - whether a later runtime-facing boundary is justified at all after those questions are resolved

## Why The Other Options Are Rejected

- `Verified`: Reject Option B because direct runtime-facing authorization would skip the unresolved collection-template and archive-adjacent planning questions that this broader boundary exists to surface.
- `Verified`: Reject Option C because `UNIT-127` already established collection-first sequencing and because the family ledger keeps `collection companion` and `PDP companion` distinct.
- `Verified`: Reject Option D because presets should consume resolved family-boundary decisions, not replace them.

## What This Boundary Does Not Authorize

- `Verified`: No runtime build.
- `Verified`: No exact runtime file list.
- `Verified`: No collection-companion implementation boundary yet.
- `Verified`: No direct reopening of `PDP companion`.
- `Verified`: No collection-template rewrite work.
- `Verified`: No archive or navigation system work.
- `Verified`: No broader motion/media, preset-pack, wider JS, or heavier commerce continuation.

## Required Constraints For The Next Approved Docs-Only Step

- `Verified`: Keep the next step documentation-only.
- `Verified`: Keep edits limited to `workspace/plans/`, `state/`, and `workspace/handoff/CURRENT_HANDOFF.md`.
- `Verified`: Keep the next step collection-first and fail closed on `PDP companion`.
- `Verified`: Require the next step to name the first narrowed collection-companion lane explicitly rather than using vague shared-family wording.
- `Verified`: Require the next step to record which collection-adjacent surfaces remain blocked, especially archive controls, template rewrites, broader JS, and heavier commerce.
- `Verified`: Require the next step to stop if it starts authorizing runtime work directly.

## Stop Conditions For The Next Approved Docs-Only Step

- `Verified`: Stop if the next planning step authorizes runtime edits directly.
- `Verified`: Stop if it normalizes collection-template rewrites or collection archive behavior as implied companion work.
- `Verified`: Stop if it reopens `PDP companion` through shared companion wording.
- `Verified`: Stop if it turns broader motion/media, preset-pack implementation, wider JS, or heavier commerce into implicit follow-ons.

## Readiness Verdict

- `Verified`: READY as a docs-only broader boundary package.
- `Inference`: The next governed unit after this boundary should remain docs-only and author a Phase 3 collection-companion controlled expansion proposal.
