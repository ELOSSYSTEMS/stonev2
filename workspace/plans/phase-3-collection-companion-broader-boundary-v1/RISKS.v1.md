# Phase 3 Collection Companion Broader Boundary Risks v1

1. `Inference`: The broader boundary could smuggle collection-template rewrites or archive controls back in through vague terms such as `navigation aids` or `collection support behavior`.
2. `Inference`: The shared settings vocabulary around `collection behavior` and `product-card behavior` could be misread as approval for heavier commerce, richer product-card behavior, or wider merchandising drift inside `collection companion`.
3. `Inference`: The package could reopen `PDP companion`, preset-pack work, or broader motion/media continuation indirectly if the next step is not kept explicitly collection-first and docs-only.
4. `Verified`: The mitigation is to keep this unit planning-only, block direct runtime authorization, keep navigation/archive/template rewrites fail-closed, and hand off only to a narrower docs-only collection-companion controlled expansion proposal.
