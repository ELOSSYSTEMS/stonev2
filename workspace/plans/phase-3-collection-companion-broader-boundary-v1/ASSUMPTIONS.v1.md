# Phase 3 Collection Companion Broader Boundary Assumptions v1

- `Verified`: `docs/architecture/stonev2-build-roadmap.md` still treats collection and PDP companion systems as unresolved Phase 3 family work that precedes preset-pack implementation.
- `Verified`: `workspace/specs/STONEV2_SECTION_FAMILY_LEDGER.md` keeps `collection companion` distinct from both merchandising and `PDP companion`, while also placing it on collection pages first.
- `Verified`: `workspace/specs/STONEV2_SETTINGS_TAXONOMY.md` shows that broader collection-companion planning now touches shared `collection behavior` and `product-card behavior` vocabularies rather than a purely isolated section proof.
- `Verified`: `UNIT-071` through `UNIT-074` proved only one narrow collection-aware runtime slice and then explicitly stopped automatic deeper collection-aware continuation.
- `Verified`: `UNIT-127` concluded that companion-family planning may reopen only in a collection-first, docs-only, fail-closed form.
- `Inference`: The first honest reopening step is therefore a broader collection-companion planning boundary that still hands off to another docs-only proposal instead of direct runtime authorization.
